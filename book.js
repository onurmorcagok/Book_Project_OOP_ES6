class Book {
    constructor(title, author, url, page, publisher, type) {

        this.title = title;
        this.author = author;
        this.url = url;
        this.page = page;
        this.publisher = publisher;
        this.type = type;
    }
}